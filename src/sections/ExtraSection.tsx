import React from "react";

const ExtraSection: React.FC = () => {
  return (
    <div className="h-screen flex items-center justify-center bg-gray-100 text-3xl font-semibold">
      Extra Section
    </div>
  );
};

export default ExtraSection;
